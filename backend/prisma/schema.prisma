generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../data/planes.sqlite"
}

model Planes {
  icao24        String  @id
  callsign      String?
  originCountry String?
  velocity      String?
  latitude      String?
  longitude     String?
}
